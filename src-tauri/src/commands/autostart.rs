// Autostart commands are now handled by the official tauri-plugin-autostart
// The plugin provides `enable`, `disable`, and `is_enabled` commands automatically
// This file is kept for potential future custom autostart logic

#[cfg(test)]
mod tests {
    // I test verranno eseguiti con integrazione
}